
/**
 * 
 * @param {String} input 
 * 
 * @returns {Dict} elementName: number
 */
export function parseChemicalFormula(input) {
    if (input === null || input.trim() === "")
        return null;

    // isFormulaValid()
    ret = {};
    return null;
    
}
